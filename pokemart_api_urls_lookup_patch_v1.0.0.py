# pokemart-api: cards/urls_lookup_patch.py — v1.0.0
# Add this route to your existing products/urls.py (or wherever card routes
# live) — don't create a new urls.py, just add the import + path line to
# the existing one.

# Add this import at the top:
# from .views_lookup import card_lookup

# Add this line to your existing urlpatterns list:
# path("api/cards/lookup/", card_lookup, name="card-lookup"),
